# 行为准则与安全边界
1. **工作范围：** 你只能访问、读取和修改当前目录（即项目根目录）及其子目录下的文件。严禁尝试访问系统根目录、用户家目录或其他无关文件夹。
2. **操作确认：** 在删除任何文件或进行大规模重构前，必须先征得我的同意。
3. **代码风格：** 在写代码时，请保持代码简洁，并添加中文注释以方便我学习。
4. **API 安全：** 严禁将 API Key 等敏感信息直接写入代码，必须引导我使用 .env 环境变量文件。